#include"main.h"
/**
 * add - a function that adds two integers and return the result.
 *
 * @i: integer number as input.
 * @c: integer number as input.
 *
 * Return: return the addition of the numbers.
 */
int add(int i, int c)
{
	return (i + c);
	_putchar('\n');
}
